VITE_API_URL=http://localhost:3005
VITE_PUMP_API_URL=http://localhost:3005
VITE_MEDICAL_API_URL=https://tshla-auth-api-container.redpebble-e4551b7a.eastus.azurecontainerapps.io
VITE_AWS_REGION=us-east-1
VITE_RDS_HOST=tshla-medical.cchoceqmctb1.us-east-1.rds.amazonaws.com
VITE_RDS_PORT=3306
VITE_RDS_DATABASE=tshla_medical
# Voice providers removed
VITE_SESSION_TIMEOUT_MINUTES=120
VITE_SESSION_WARNING_MINUTES=5
VITE_ENABLE_HIPAA_MODE=true
VITE_ENABLE_AUDIT_LOGGING=true
AWS_REGION=us-east-1
AWS_BUCKET_NAME=tshla-medical-frontend
VITE_ENABLE_SPEECH=true
VITE_ENABLE_AI_PROCESSING=true
VITE_ENABLE_TRANSCRIBE_MEDICAL=true

# Deepgram Configuration (Primary STT - Medical Model with Official SDK)
VITE_DEEPGRAM_API_KEY=your_deepgram_api_key_here
VITE_DEEPGRAM_MODEL=nova-2-medical
VITE_DEEPGRAM_LANGUAGE=en-US
VITE_DEEPGRAM_TIER=enhanced
VITE_PRIMARY_STT_PROVIDER=deepgram

# Deepgram SDK Configuration - Replaces Azure due to credit/quota issues
VITE_USE_DEEPGRAM_SDK=true

# ============================================
# SUPABASE CONFIGURATION (Primary Database & Auth)
# ============================================
# Get these from: https://supabase.com/dashboard → Settings → API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here
# Service role key (NEVER expose in frontend, backend only!)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Stripe Payment Processing (for pump reports)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_51example...  # NEED TO CONFIGURE
STRIPE_SECRET_KEY=sk_test_example...              # NEED TO CONFIGURE - Backend only
STRIPE_WEBHOOK_SECRET=whsec_example...            # NEED TO CONFIGURE - Backend only

# Provider Email Delivery
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASSWORD=SG.example...                      # NEED TO CONFIGURE - SendGrid API key
FROM_EMAIL=noreply@tshla.ai

# Report Enhancement Settings
VITE_REPORT_BASE_URL=https://www.tshla.ai
VITE_ENABLE_PAYMENT_FLOW=true
VITE_ENABLE_PROVIDER_DELIVERY=true
VITE_REPORT_PRICE_CENTS=999

# AWS Credentials removed (Bedrock and Transcribe Medical no longer used)
# Azure Speech Services removed
# Primary speech provider: Deepgram
VITE_PRIMARY_SPEECH_PROVIDER=deepgram

# Azure Database removed - using AWS RDS or local database
# Database service connection variables - UPDATE WITH YOUR NEW DATABASE
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=tshla_medical_local
DB_USER=root
DB_PASSWORD=
DB_SSL=false

# Primary database provider switched to local/AWS
VITE_PRIMARY_DB_PROVIDER=local
PRIMARY_DB_PROVIDER=local
DATABASE_TYPE=mysql
USE_MYSQL=true

# Azure OpenAI removed - using AWS Bedrock or OpenAI direct
# Primary AI provider switched to AWS
VITE_PRIMARY_AI_PROVIDER=aws

# Twilio services removed

# Medical Authentication API Configuration
# Unified JWT Secret for all services
JWT_SECRET=tshla-unified-jwt-secret-2025-enhanced-secure-key
# Legacy for backward compatibility (remove after migration)
MEDICAL_JWT_SECRET=tshla-unified-jwt-secret-2025-enhanced-secure-key

# ============================================
# AUTHENTICATION (Supabase Auth handles all passwords)
# ============================================
# NOTE: DO NOT hardcode passwords. Users are managed in Supabase Auth.
# Admin accounts created via Supabase Dashboard → Authentication → Users

# OpenAI API Key - Get from https://platform.openai.com/api-keys
VITE_OPENAI_API_KEY=your_openai_api_key_here
