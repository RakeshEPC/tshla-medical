'use client';
export default function BuildStamp() {
  // Super-visible build marker; update the text when troubleshooting.
  return <div className="mt-2 text-[10px] text-gray-500 select-none">build: allowlist-v4</div>;
}
