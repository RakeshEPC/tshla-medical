#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# TSHLA Medical - Pre-Commit Hook
# Prevents committing broken code

echo "🔍 Running pre-commit validation..."
echo ""

# Check staticwebapp.config.json location
if [ ! -f "public/staticwebapp.config.json" ]; then
  echo "❌ staticwebapp.config.json must be in public/ folder, not root!"
  echo "   This will cause 404 errors on /admin/* routes in production."
  echo ""
  echo "   Fix: mv staticwebapp.config.json public/"
  echo ""
  exit 1
fi
echo "✅ staticwebapp.config.json in correct location (public/)"

# Check for accidentally committed credentials (exclude dev scripts, docs, and comparison checks)
if git diff --cached -- ':!start-dev.sh' ':!stop-dev.sh' ':!*.md' | grep -i "DB_PASSWORD.*=.*[^'']" | grep -v "DB_PASSWORD=\$" | grep -v 'DB_PASSWORD=""' | grep -v '===' | grep -v '!=='; then
  echo "❌ Found hardcoded credentials in staged files!"
  echo "   Never commit passwords directly in code."
  echo ""
  exit 1
fi
echo "✅ No hardcoded credentials detected"

# Check for deprecated auth service imports
echo ""
echo "Checking for deprecated auth service imports..."
DEPRECATED_IMPORTS=$(git diff --cached --name-only | grep -E '\.(ts|tsx)$' | grep -v "pumpAuth\.service\.ts$" | xargs grep -l "from.*pumpAuth\.service" 2>/dev/null || true)
if [ -n "$DEPRECATED_IMPORTS" ]; then
  echo "❌ Found imports of deprecated pumpAuth.service!"
  echo ""
  echo "   Files with deprecated imports:"
  echo "$DEPRECATED_IMPORTS" | sed 's/^/     - /'
  echo ""
  echo "   ⚠️  pumpAuth.service is deprecated and will be removed."
  echo "   ✅ Use supabaseAuth.service instead."
  echo ""
  echo "   Migration example:"
  echo "     // ❌ Old (deprecated):"
  echo "     import { pumpAuthService } from './pumpAuth.service';"
  echo ""
  echo "     // ✅ New (correct):"
  echo "     import { supabaseAuthService } from './supabaseAuth.service';"
  echo ""
  exit 1
fi
echo "✅ No deprecated auth imports detected"

# Check for console.log in server code (HIPAA compliance)
echo ""
echo "Checking for console.log statements in server code (HIPAA)..."
# Exclude comment lines (starting with * or //) and deprecated services
# Also exclude CLI debug scripts (check-*.js)
CONSOLE_FOUND=$(git diff --cached --name-only | grep -E '^server/.*\.(js|ts)$' | grep -v '_deprecated_external_services/' | grep -v 'check-.*\.js$' | xargs grep "console\." 2>/dev/null | grep -v "^\s*\*" | grep -v "^\s*//" | cut -d: -f1 | sort -u || true)
if [ -n "$CONSOLE_FOUND" ]; then
  echo "❌ console.log statements found in server code!"
  echo ""
  echo "   Files with console statements:"
  echo "$CONSOLE_FOUND" | sed 's/^/     - /'
  echo ""
  echo "   ⚠️  HIPAA Compliance: console.log exposes PHI in production logs"
  echo "   ✅ Use logger instead:"
  echo ""
  echo "   Migration examples:"
  echo "     // ❌ Old (HIPAA violation):"
  echo "     console.log('Processing patient:', patientName);"
  echo ""
  echo "     // ✅ New (HIPAA compliant):"
  echo "     logger.info('Category', 'Processing patient', { patientId });"
  echo ""
  echo "   See: HIPAA-SAFE-LOGGING-GUIDE.md"
  echo ""
  exit 1
fi
echo "✅ No console.log statements in server code"

# Check for patient_id misuse in schedule components (TSH ID bug prevention)
echo ""
echo "Checking for patient_id misuse in schedule components..."
SCHEDULE_FILES=$(git diff --cached --name-only | grep -E 'Schedule.*\.(ts|tsx)$' | grep -E '(components|pages)/' || true)
if [ -n "$SCHEDULE_FILES" ]; then
  # Check if any schedule file uses patient_id for tsh_id or tshId
  MISUSE_FOUND=$(echo "$SCHEDULE_FILES" | xargs grep -n -E "(tsh_?id|tshId).*patient\.patient_id|patient\?\.patient_id.*tsh" 2>/dev/null | grep -v "internal_id" || true)
  if [ -n "$MISUSE_FOUND" ]; then
    echo "❌ Found patient_id being used for TSH ID display!"
    echo ""
    echo "   Files with incorrect field usage:"
    echo "$MISUSE_FOUND" | sed 's/^/     /'
    echo ""
    echo "   ⚠️  CRITICAL ERROR: This will show 8-digit numbers instead of formatted TSH IDs"
    echo ""
    echo "   What's wrong:"
    echo "     patient.patient_id = \"99364924\" (8-digit internal ID)"
    echo "     patient.tshla_id = \"TSH 972-918\" (formatted TSH ID for display)"
    echo ""
    echo "   ❌ WRONG:"
    echo "     tsh_id: patient?.patient_id  // Shows \"99364924\""
    echo ""
    echo "   ✅ CORRECT:"
    echo "     tsh_id: patient?.tshla_id    // Shows \"TSH 972-918\""
    echo ""
    echo "   Or even better, use the helper function:"
    echo "     import { getDisplayTshId } from '@/utils/patient-id-formatter';"
    echo "     tsh_id: getDisplayTshId(patient)"
    echo ""
    echo "   See: TSH_ID_FORMAT_FIX.md"
    echo "   See: src/types/unified-patient.types.ts"
    echo ""
    exit 1
  fi
  echo "✅ No patient_id misuse detected in schedule components"
else
  echo "✅ No schedule files modified"
fi

# Check TypeScript compiles
echo ""
echo "Running TypeScript check..."
npm run typecheck
if [ $? -ne 0 ]; then
  echo ""
  echo "❌ TypeScript errors found. Fix them before committing."
  echo ""
  exit 1
fi

echo ""
echo "✅ All pre-commit checks passed!"
echo "   Proceeding with commit..."
echo ""
